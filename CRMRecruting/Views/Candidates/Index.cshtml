@model IEnumerable<CRMRecruting.Models.Candidate>

@{
    ViewBag.Title = "Index";
}

<h2>Candidates - report</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            Id
        </th>
        <th>
            Name
        </th>
        <th>
           University
        </th>
        
      
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.University)
        </td>
      
        
       
    </tr>
}

</table>
<div style="padding: 10px; border:1px solid black">
    <div><a href="@Url.Action("CandidatesReport",new { id = "PDF" })">Get report PDF</a></div>
</div>